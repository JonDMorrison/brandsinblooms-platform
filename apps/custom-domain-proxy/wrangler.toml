name = "custom-domain-proxy"
main = "src/index.ts"
compatibility_date = "2025-09-25"
compatibility_flags = ["nodejs_compat"]

# Production environment
[env.production]
name = "client-brands-in-blooms-proxy-production"
vars = { ENVIRONMENT = "production" }

# Staging environment
[env.staging]
name = "client-brands-in-blooms-proxy-staging"
vars = { ENVIRONMENT = "staging" }

# Secrets (set via: wrangler secret put <SECRET_NAME>)
# - ORIGIN_ENDPOINT: The origin server URL (e.g., https://blooms-staging.cc or https://blooms.cc)
#
# Example:
# wrangler secret put ORIGIN_ENDPOINT --env production

# Routes will be configured via Cloudflare dashboard
# after domain is added to your Cloudflare account
